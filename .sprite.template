{{#sprites}}
.icon--{{name}} {
  content: '';
  width: {{px.width}};
  height: {{px.height}};
  background: url(images/{{{escaped_image}}}) no-repeat {{px.offset_x}} {{px.offset_y}};
  display: inline-block;
}
{{/sprites}}
